#!/usr/bin/env node
import { createLanguageServer } from './src/lang-server/server.mjs';

// Create and start server with default (real) build functions
const server = createLanguageServer();
server.listen();
